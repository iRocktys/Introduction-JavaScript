// Criando variáveis para armazenar dados
const nome = "Maria";
let idade = 30;
const cidade = "São Paulo";
let estaAtivo = true;

// Exibindo as variáveis no console do navegador
console.log(nome);
console.log(idade);
console.log(cidade);
console.log(estaAtivo);

// Tentando mudar o valor de 'idade' (o que é permitido com let)
idade = 31;
console.log("Nova idade:", idade);
